services:

  # ── PostgreSQL ────────────────────────────────────────────────────────────
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER:     ${POSTGRES_USER:?POSTGRES_USER must be set in .env}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set in .env}
      POSTGRES_DB:       ${POSTGRES_DB:?POSTGRES_DB must be set in .env}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:?POSTGRES_USER must be set in .env}"]
      interval: 10s
      timeout: 5s
      retries: 5
    # NOT exposed to the host — only accessible inside the Docker network

  # ── FastAPI backend ───────────────────────────────────────────────────────
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:?POSTGRES_USER must be set in .env}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set in .env}@db:5432/${POSTGRES_DB:?POSTGRES_DB must be set in .env}
      DOCS_ENABLED: ${DOCS_ENABLED:-false}
    # Seed the shared volume with static files from the image, then start the app.
    # Mounting the volume at /app/app/static would shadow the files baked into the
    # image, so we use a separate path (/app/static_serve) and copy into it once.
    command: >
      sh -c "cp -rn /app/app/static/. /app/static_serve/ && exec uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2"
    volumes:
      - static_files:/app/static_serve
    depends_on:
      db:
        condition: service_healthy
    # NOT exposed to the host — nginx is the only entry point

  # ── Nginx reverse proxy / frontend ───────────────────────────────────────
  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - "80:80"        # Public HTTP (frontend + API through same domain)
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - static_files:/app/static_serve:ro   # Serve /static/* without hitting Python
    depends_on:
      - backend

  # ── Adminer (dev only) ────────────────────────────────────────────────────
  adminer:
    image: adminer:latest
    restart: unless-stopped
    ports:
      - "8080:8080"   # DB GUI — only bind this in dev, never in production
    depends_on:
      - db
    profiles:
      - dev            # Only starts with: docker compose --profile dev up

volumes:
  pgdata:
  static_files:        # Shared volume: backend writes, nginx reads
